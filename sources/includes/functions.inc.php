<?php


emptyInputSignUp($name, $email,$password, $username, $passwordRepeat){
    $result;
    if(empty($name) || empty($email) || empty($password)|| empty($username)|| empty($passwordRepeat) ){
        $results = true;
    }
}